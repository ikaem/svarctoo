package com.imkaem.android.svarctoo.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors - Green/Teal theme for expense tracking
val Primary = Color(0xFF2E7D32)  // Green
val PrimaryLight = Color(0xFF66BB6A)  // Light Green

// Secondary colors - Orange/Amber for accents
val Secondary = Color(0xFFF57C00)  // Orange
val SecondaryLight = Color(0xFFFFB74D)  // Light Orange

// Tertiary colors - Red for spending/negative
val Tertiary = Color(0xFFC62828)  // Red
val TertiaryLight = Color(0xFFEF5350)  // Light Red

// Neutral colors
val Background = Color(0xFFFAFAFA)  // Off-white
val Surface = Color(0xFFFFFFFF)  // Pure white

// Dark mode neutral colors
val DarkBackground = Color(0xFF121212)  // Very dark gray (dark mode background)
val DarkSurface = Color(0xFF1E1E1E)  // Dark gray (dark mode surface)
val DarkOnBackground = Color(0xFFE0E0E0)  // Light gray text on dark background
val DarkOnSurface = Color(0xFFE0E0E0)  // Light gray text on dark surface

// Text colors
val OnPrimary = Color(0xFFFFFFFF)  // White text on primary
val OnSecondary = Color(0xFFFFFFFF)  // White text on secondary
val OnBackground = Color(0xFF212121)  // Dark text on background
val OnSurface = Color(0xFF212121)  // Dark text on surface