package com.imkaem.android.svarctoo.core.di

import dagger.Module
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent

/**
 * Core dependencies module for shared application-wide dependencies.
 * 
 * This module is a placeholder for future shared dependencies such as:
 * - Database instances
 * - Shared preferences
 * - Logging/Analytics
 * - Network clients
 * - Other application-level resources
 */
@Module
@InstallIn(SingletonComponent::class)
object CoreModule {
    // Shared dependencies will be added here as the app grows
}
